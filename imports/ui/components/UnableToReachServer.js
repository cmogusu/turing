import React from 'react';

function UnableToReachServer() {
  return {
    <div>
      <h2>Ooops, servers unreachable</h2>
      <p>We can't connect to our servers right now</p>
    </div>
  };
}

export default UnableToReachServer;