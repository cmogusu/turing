import React from 'react';
import { Meteor } from 'meteor/meteor';

function withFavourites(Component) {
  class MyClass extends React.Component {
    render() {
      beers

      return <Component />;
    }
  };

  return MyClass;
}

export default withFavourites;